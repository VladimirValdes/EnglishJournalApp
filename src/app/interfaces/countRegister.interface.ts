export interface CountRegister {
  verbsTotal:        number;
  adjectivesTotal:   number;
  phrasalverbsTotal: number;
  prepositionsTotal: number;
  connectorsTotal:   number;
}

export interface DateFilter {
  startDate: string;
  endDate:   string;
}
